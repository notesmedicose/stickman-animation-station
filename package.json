{
  "name": "stickman-animation-station",
  "version": "1.0.0",
  "description": "Professional Stickman Animation Studio",
  "main": "index.js",
  "scripts": {
    "cap:sync": "npx.cmd cap sync",
    "cap:copy": "npx.cmd cap copy",
    "cap:open": "npx.cmd cap open android",
    "cap:assets": "npx.cmd @capacitor/assets generate --android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
